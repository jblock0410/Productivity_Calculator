/*
function addTotalTime1() {
    let timeEntry = document.getElementsByClassName('timeEntry');
    let timeValue = timeEntry.textContent;
    for (let i = 0; i < timeEntry.length; i++) {
        timeValue[i] += timeValue;
        return timeValue;
    }
        let p = document.getElementsByClassName('total');
        p.value = timeValue;    
};

function addTotalTime (button) {
    let submitButtonId = button.id;
    switch (submitButtonId) {
        case 'strategicSubmit':
            addTotalTime1(submitButtonId);
            break;
    }
}
*/

const section = document.querySelectorAll('submit');

/*
function addTotalTime1() {
    let p = document.createElement('p');
    p.setAttribute('class', 'totalTime');
    let timeInput = p.textContent;
    for (let i = 0; i < li.length; i++) {
        timeInput[i] = p;
        p += timeInput[i];
    }
    submit.appendChild(p);
}

function addTotalTime (button) {
    let submitButtonId = button.id;
    switch (submitButtonId) {
        case 'strategicSubmit':
            addTotalTime1(submitButtonId);
            break;
    }
}
*/
/*
function sumTotal() {
    let p = document.createElement('p');
    let section = document.getElementsByTagName('section');
    let sum = [];
    sum.push(input.value);
    p = sum.reduce(input.value);
}
*/






/*
function sumTotalButton (button) {
    let buttonId = button.id;
    switch (buttonId) {
        case 'strategicSubmit':
        sumTotal(buttonId);
        break;
    }
}
*/